{
  "name": "a",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "git clean -xdf node_modules .turbo bun.lock",
    "clean:workspaces": "turbo run clean",
    "dev:blog": "turbo watch dev -F @a/blog -F @a/be",
    "dev:chat": "turbo watch dev -F @a/chat -F @a/be",
    "dev:movie": "turbo watch dev -F @a/movie -F @a/be",
    "dev:org": "turbo watch dev -F @a/org -F @a/be",
    "fix": "sort-package-json **/package.json && oxlint --fix --fix-suggestions --quiet && bun lint:fix -- --quiet && biome check --fix --diagnostic-level=error && bun typecheck && bun run build",
    "postinstall": "bun lint:ws",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache && oxlint --quiet",
    "lint:fix": "turbo run lint --continue -- --fix --cache --cache-location .cache/.eslintcache",
    "lint:ws": "sherif",
    "test:all": "bun fix && CONVEX_TEST_MODE=true bun --cwd packages/be with-env convex dev --once && bun --cwd packages/be with-env convex env set CONVEX_TEST_MODE true && SKIP_CONVEX_ENV_TOGGLE=1 turbo run test; EXIT=$?; bun --cwd packages/be with-env convex env remove CONVEX_TEST_MODE; exit $EXIT",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@types/bun": "latest",
    "depcheck": "latest",
    "dotenv-cli": "latest",
    "oxlint": "latest",
    "sherif": "latest",
    "sort-package-json": "latest",
    "ts-unused-exports": "latest",
    "turbo": "latest",
    "typescript": "latest"
  },
  "packageManager": "bun@1.3.9"
}
